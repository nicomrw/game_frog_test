# The Croaking Veil Prototype Setup

## 1. Start the Server

```bash
cd server
npm install
node server.js
```

## 2. Start the Client

```bash
cd client
npm install
npm run dev
```

The game should now be running in your browser, connecting to the backend server!
